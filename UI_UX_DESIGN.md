# 课表 App · UI/UX 设计方案（校园贴纸风）

> 版本：v3.0（贴纸风改版）· 2026-09-26（随 v1.6 更新）
> 依据：本项目自有「贴纸设计系统」，与官网 `website/`（`website/assets/css/main.css`）同一套 token
> 现状约束：compileSdk 35、AGP 8.7.3、Kotlin 2.1.21、compose-bom 2024.06.00
> 双版本线：v2.x 主线包名 `com.saltfish.simple`；v1.7.x LTS 包名 `com.example.composeapp`。两线 UI 一致。

## 一、设计基调

- **校园贴纸风**：米黄纸面打底，墨线描边 + 硬投影的贴纸卡片，荧光笔高亮、虚线 chip、
  微微歪头的贴纸徽章——整只 App 像一本手帐
- **唯一视觉基准**：官网 CSS token（`website/assets/css/main.css`），App 侧集中在
  `ui/theme/Color.kt` 定义（亮/暗两套），经主题层下发到全局
- **核心 token**：

| token | 亮色 | 暗色 | 用途 |
|---|---|---|---|
| paper（页面底） | `#FFF8EC` | `#201D2B` | 全应用背景（纸面） |
| card（贴纸面） | `#FFFFFF` | `#302B40` | 所有卡片/弹层 |
| ink（墨色） | `#2D2A45` | `#F0ECDC` | 正文 + 所有描边 |
| hl（荧光黄） | `#FFD666` | `#FFD666` | 高亮、选中态、徽章（双主题同值） |
| brand（品牌色） | `#333464` | `#333464` | 主按钮底、选中胶囊底 |

- **形状与投影**：描边 2.5dp（小元素 1.5dp）；硬投影 `4dp 4dp 0 墨色`（大贴纸 6dp、小元素 2dp），
  一律 `drawBehind` 画硬边矩形，不用模糊阴影；圆角 16dp（按钮 14dp、课程块 8dp、胶囊 99dp）
- **动效中枢**：`ui/theme/AppMotion` 提供全局弹簧四规格（spatial / spatialFast / effects /
  effectsFast），所有自定义动画统一走它
- **触感**：`ui/theme/Haptics` 三档（tick / click / heavy），关键操作全部接线

## 二、信息架构与导航

3 个一级目的地，Scaffold + **自绘迷你底栏**：

```
┌ 课表(周视图,首页) ┐
│ 今日               │ ← 底栏四项：课表 / 今日 / 校园 / 我的
└ 我的(设置)        ┘
```

- 底栏：56dp 高，玻璃模式为悬浮岛形态（无投影、加重玻璃底）；选中项为品牌底荧光字胶囊（48×32dp）
- 胶囊可**横向拖拽**：跟手移动、跨槽位轻震、松手就近吸附并切页；点击直接切页 + 图标 1.15x 回弹
- 页面切换：`AnimatedContent` 方向性转场（向右切从右滑入），规格取 AppMotion
- 二级覆盖页（课表管理/作息/小组件/关于/隐私）：`OverlayPage` 容器——深度缩放 + 淡切进入退场；
  主界面常驻组合（alpha 淡隐）保证**返回时保留滚动位置**；内容下方垫触摸拦截层防穿透
- **右滑返回全应用统一**：`ui/theme/Anim.kt` 的 `SwipeBackBox` 为唯一侧滑实现，
  在 Initial pass 先于页面内容判定边缘手势——所有二级/三级页（含 WebView 页）行为一致；
  系统返回键、右滑、页内关闭按钮三者走同一个 onBack

## 三、核心页面

### 1. 课表页（周视图，主屏）——去网格线

- 顶行：「第 N 周」同行内联（titleLarge Bold）+ 日期范围副行；右侧分享 / 新增 / 周选择三个图标；
  顶行与网格之间**无分隔线**
- 周切换：左右滑动网格（HorizontalPager）；点击标题弹出周选择面板（周次 chip + 课表切换）
- 表头：7 列（受"显示周末"开关控制）；**今日徽标为荧光黄底 + 墨线描边圆徽章**，星期红字
- **网格无任何表格线**：无节次分隔线、无表头分隔线、无空格子框——课程块直接贴在纸面上
- 课程块 = 迷你贴纸：**五组粉彩色 × 深浅共 10 组色板**（官网同源）按课程名去重分配，
  块体为粉彩底 + 1.5dp 墨线描边 + 2dp 硬投影 + 8dp 圆角；内容为课程名（≤6 行）+ ★○●◇ 类型标记
  + 教师@教室；非本周淡化、冲突分槽、当前时间红线；长按拖拽调课（落位重震、自动避让最近空位）
- 自定义背景（可选）：所选图片直接铺在纸面层，贴纸卡片原样压在图上（无模糊、无高光）

### 2. 今日页

正在上课 Hero 卡（进度条）、下一节课卡（倒计时）、今日时间轴列表；已结束项淡化。

### 3. 我的页

分组卡片 + 卡内细分隔线，层次化而非零散堆放：

| 分组 | 内容 |
|---|---|
| 学期信息（视觉重心） | 第 N 周/倒计时大字 + 开学日期（DatePicker）+ 学期周数（对话框） |
| 课表 | 导入文件、作息时间、课表管理、桌面小组件、同步到系统日历、清空日历课程、导出 .ics |
| 显示 | 显示周末、非本周淡化、自定义纸面背景（选图/清除） |
| 外观 | 深色模式三选（跟随系统/亮色/暗色） |
| 提醒 | 课前提醒开关（展开：提前 5/10/15/20 分钟、测试通知） |
| 数据 | 课表统计 + 清除当前课表（danger 红） |
| 关于 | 版本、隐私政策 |

- 设置存储为 SharedPreferences（`SettingsRepository` Flow 收集），**非 DataStore**

### 4. 关于页 / 隐私政策页

- 关于：真实应用图标头部 + 主要功能单卡分组 + **更新记录折叠收纳**（点击展开，
  当前版本默认展开，箭头旋转动画）+ 兼容性 + 开发者 + 开源仓库链接
- 隐私：核心离线承诺卡 + **权限清单**（逐项说明用途与触发时机）+ 不联网承诺
  （无 INTERNET 权限为系统层硬限制）+ 数据存储位置 + 兼容性说明

## 四、关键流程

- **导入**：教务网页导入（学校选择 → 适配器 → 内嵌浏览器）为主；
  SAF 选文件 / 系统分享直达为辅 → 前台服务本地解析 → 导入确认弹窗
- **系统日历**：申请 READ/WRITE_CALENDAR → 创建/定位「不挂科课表」本地日历 →
  清空旧事件后批量写入（含提醒规则）；清空操作有确认弹窗；绝不触碰用户其他日历
- **课程编辑/新增**：ModalBottomSheet 表单（名称/教师/教室 + 星期 + 节次 + 周次 chip 流）

## 五、动效与触感

- AppMotion 四规格全量接入：底栏胶囊吸附、页签转场、周数滚动、二级页 OverlayPage 转场、
  更新记录折叠、侧滑跟手回弹
- 底栏选中图标 1.15x 回弹；主界面在二级页打开时 alpha 淡隐
- Haptics 接线约 20 处：底栏点击/跨槽位/吸附（tick）、侧滑武装（tick）、课表拖起（tick）/落位
  （heavy）、保存/添加/切换/复制/同步/清空（click）、删除/清除（heavy）、开关与芯片（tick）

## 六、无障碍与适配

- 触控目标 ≥48dp；课程块合并单节点语义
  `contentDescription = "周一 1-2节 模拟电子线路 环宇楼A404"`
- 暗色模式：暗纸面 + 亮墨描边双套 token，主题根部统一提供 LocalContentColor
- 支持系统字体缩放；手机单列布局

## 七、与官网的一致性

- 官网（`website/`）是贴纸设计系统的第一实现，App 是第二实现；token 改动两边同步
- 组件对应关系：官网 `.sticker` ↔ App `StickerCard`（规划中，见
  `docs/贴纸风整体改版-v1.6-开发方案.md` §3）；`.badge` ↔ 贴纸徽章；`.chip` ↔ 虚线 chip；
  `.hl-pen` ↔ 荧光笔高亮文字
